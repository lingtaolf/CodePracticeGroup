#include <string.h>


using namespace std;
class Solution {
public:
    bool isLongPressedName(string name, string typed) {
        int start_pos = 0;
        int str_length= 1;
        string substr = "";

        while()
        {
            if(name[start_pos] == name[start_pos+str_length])
            ++str_length;
        }

        substr = name.substr(start_pos, str_length);
        
          
        

    }
};
